import random
# let a user enter which of these three values
userPlay = input('Enter a play: ')
# you also need to randomly select an option for the computer/opponent to play.
compPlayGen = random.randrange(1,4)
  # maybe store opponents plays in a dicitionary
compPlay = {
  'Rock' : 1,
  'Paper' : 2,
  'Scissors' : 3,
}
# Display both of these selections
if compPlayGen = 1:
  compPlayGen = compPlay('Rock')
# display a message that decides if the user won or lost (or if there was a tie!)

# Commento